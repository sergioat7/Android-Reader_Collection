<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="aragones.sergio.readercollection">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".ReaderCollectionApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        tools:ignore="AllowBackup">
        <activity
            android:name=".activities.BookDetailActivity"
            android:theme="@style/Theme.ReaderCollection.NoActionBar.BookDetail"
            android:parentActivityName=".activities.MainActivity" />
        <activity
            android:name=".activities.MainActivity"
            android:theme="@style/Theme.ReaderCollection.ActionBar"
            android:launchMode="singleTask" />
        <activity
            android:name=".activities.RegisterActivity"
            android:theme="@style/Theme.ReaderCollection.NoActionBar" />
        <activity
            android:name=".activities.LoginActivity"
            android:theme="@style/Theme.ReaderCollection.NoActionBar" />
        <activity
            android:name=".activities.LandingActivity"
            android:theme="@style/Theme.ReaderCollection.NoActionBar"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>